## Fact

- 2번째 주 코드숨 강의를 들었다. 강의를 듣고 실습을 하면서 저번 주 스프링 MVC을 들은게 굉장히 도움이 되었고 실습을 하면서 좀 더 스프링을 잘 사용할 수 있는 자신이 들엇다.

- 저번 주에 다 못들었던 인프런 스프링 MVC 1편을 마저 들었다. 이번 강의를 들으면서 스프링 MVC의 `DispatcherSerlvet`에 관해서 남들에게 어느 정도 설명할 정도로 알게 된거 같다. 그리고 역시 갓영한

## Feelings

- 저번 주보다 뭔가 열정이 식은거 같다. 영한님께서 언젠간 열정은 식으니까 시스템(루틴)을 만들라는 말씀을 하셨는데. 회고만은 꾸준히 작성해야 겠다

- 오늘 목표를 한 일을 다 하지 못했다. 시간은 똑같이 쓴거 같은데 왜 하지 못했을까 고민 했다. 먼저 저번 주 보다 열정이 식어 집중력도 많이 떨어진거 같다. 한번 내일도 이 상태가 반복이 되면 조치를 취해야겠다.

- 코드 리뷰 중 병렬성에 관한 피드백을 받아서 `AtomicLong` 과 `ConcurrentHashMap` 을 사용하게 되었는데 제대로 잘 알지 못하는 것 같다고 느꼇다. 멀티 쓰레드에 대해 좀 더 공부를 해야겠다고 느꼇다.

## Findings

- 코드 리뷰 중 `setter` 를 빼고 변경하라는 피드백이 있었는데 `setter`를 뺀 객체는 `jackson` 을 이용하면 안되지 않나? 하고 알아 보았다.
  결론은 리플랙션이라는 기술을 사용해 필드 값을 넣어줘서 `setter` 나 `getter` 나 `public field` 만 있으면 된다는 걸 알았다.

## 내일 할 것

- 멀티 쓰레드 뽀개기

- 일일 회고 작성하기
